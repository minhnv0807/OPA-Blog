services:
  app:
    build: .
    container_name: blog_opa_app
    restart: unless-stopped
    environment:
      NODE_ENV: production
      MONGODB_URI: mongodb+srv://nguynanh13_db_user:nguyen123@cluster0.fvly7tb.mongodb.net/blog-opa?retryWrites=true&w=majority&appName=Cluster0
      AUTH_SECRET: ${AUTH_SECRET:-opa-blog-secret-key-2026-change-me}
      NEXTAUTH_URL: ${NEXTAUTH_URL:-http://localhost:3000}
      HOSTNAME: "0.0.0.0"
    ports:
      - '3000:3000'
    healthcheck:
      test: wget -qO- http://localhost:3000 > /dev/null || exit 1
      interval: 15s
      timeout: 5s
      retries: 5
